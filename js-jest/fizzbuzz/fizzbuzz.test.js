const fizzbuzz = require('./fizzbuzz');

test('verify test framework runs', () => {
    throw new Error('A failing test confirms the test framework is running!');
});

/*
test('a number divisible by only 3 should say Fizz', () => {
    expect(fizzbuzz(3)).toBe('Fizz');
});
*/
