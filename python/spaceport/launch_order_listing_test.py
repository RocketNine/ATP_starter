import pytest

# TODO - Use the Stub Recipe to test that launches are sorted correctly
def test_launches_are_sorted_by_destination_when_destinations_are_unique():
    # Step 1. Create LaunchInfoProviderStub (that extends SpacelineLaunchInfoProvider)

    # Step 2 & 3 & 4. Create SUT - SpaceportDepartureBoard, using Constructor Injection
    # Exercising the sorting behavior happens during construction of the System Under Test

    # Step 5. Verify the results are sorted correctly
    pytest.fail("TODO - Write asserts for the info sorting behavior (and then implement sorting behavior)");